rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // دالة التحقق من تسجيل الدخول
    function isSignedIn() {
      return request.auth != null;
    }

    // دالة التحقق من كونه المسؤول المالك بالبريد الإلكتروني
    function isOwnerAdmin() {
      return isSignedIn() && request.auth.token.email == "admin@al-bashmohandes.com";
    }

    // --- قاعدة المسؤول: له كل الصلاحيات المطلقة على كل شيء ---
    match /{allPaths=**} {
      allow read, write, delete: if isOwnerAdmin();
    }

    // --- البيانات العامة: مفتوحة للقراءة والبحث للجميع لتجنب ايرور الدخول ---
    match /notifications/{id} {
      allow read, list: if true;
    }

    match /courses/{courseId} {
      allow read, list: if true;
      
      // المحتوى الداخلي للكورسات (دروس، فيديوهات)
      match /{allSubcollections=**} {
        allow read, list: if isSignedIn();
      }
    }

    // --- بيانات الطلاب: كل طالب يتحكم في ملفه الشخصي فقط ---
    match /students/{studentId} {
      allow read, write: if isSignedIn() && request.auth.uid == studentId;
      
      // البيانات الفرعية للطالب (الاشتراكات والتقدم)
      match /{allSubcollections=**} {
        allow read, write: if isSignedIn() && request.auth.uid == studentId;
      }
    }

    // الأكواد (للتفعيل)
    match /access_codes/{id} {
      allow read, update: if isSignedIn();
    }
    
    // دعم استعلام المجموعات (Collection Groups) للمسؤول فقط
    match /{path=**}/enrollments/{id} {
      allow read, list: if isOwnerAdmin();
    }
    
    match /{path=**}/quiz_attempts/{id} {
      allow read, list: if isOwnerAdmin();
    }
  }
}